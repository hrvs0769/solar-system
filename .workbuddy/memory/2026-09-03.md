# 2026-09-03

## 项目全面解读（只读调研，未改代码）

- 通读项目：package.json / index.html / src 全部入口 / docs 9 篇文档 / scripts / tests。
- 运行 `npm test`：12/12 通过（astro 回归 + clock + zoom/rates）。
- 关键事实（供后续会话直接引用）：
  - 项目 = 「璀璨太阳系」，面向中学物理课堂的浏览器 3D 太阳系教学系统，two 入口（index.html 桌面、mobile.html 手机），两套构建（`dist/` 在线、`dist-offline/` 双击离线，676KB 单文件 + textures/）。
  - 技术栈：three 0.170（WebGL2）+ astronomy-engine 2.1.19（VSOP87 + 月表）+ vite 6 + vite-plugin-singlefile。
  - 五个教学模块：orbit-view 全景 / moon-phases 月相 / tides 潮汐 / eclipse 日月食 / seasons 四季。
  - 开发进度：P0–P7 完成 + 7 轮收尾（最新为"真实距离比例可用化 + 月地三态澄清 + 距离核对"）。
  - **无 git 仓库**（最大风险，仅靠 docs/开发日志.md 手工记录进度）。
  - 无 README；tmp-pup(16M) / tmp-qr(2.6M) 为遗留临时目录。
  - 上线阻断项：11 处科普文案 reviewStatus='pending'，待物理老师审校。
  - 测试体系：单测 12（node --test）、e2e 47（puppeteer-core + 系统 Chrome + swiftshader）、手机自检、verify-a1、check-switch；`npm run test:all` 一键全量。

## 身份建档（bootstrap 完成）

- 用户称呼：**HR**；我的名字：**小B**（B记 / B仔 亦可）。
- 已写入并更新：`~/.workbuddy/IDENTITY.md`、`USER.md`、`SOUL.md`（新增 "How We Talk" 段），跨项目约定写入 `~/.workbuddy/MEMORY.md`。
- `~/.workbuddy/BOOTSTRAP.md` 已删除，引导流程结束。
